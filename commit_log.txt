ssx | 2026-05-20 | fix: 配置文件修改 
ssx | 2026-05-20 | fix 
ssx | 2026-05-20 | Merge branch 'main' of https://github.com/Lawer09/nxpanel 
ssx | 2026-05-20 | feat(admin-enum): 新增项目绑定 AppID 枚举接口并支持关键字过滤 - 在 `EnumController` 新增 `getAppIds` 方法，提供 `GET /enum/app-ids` 接口
- 数据来源 `project_user_app_map.app_id`，实现去重、去空值、按 `app_id` 升序返回
- 返回统一枚举结构：`appId` / `value` / `label`
- 增加 `EnumAppIdsRequest` 参数校验接入
- 支持 `keyword` 模糊过滤（不区分大小写）
- 使用缓存键 `enum:project_user_app_ids` 缓存枚举结果，提升查询性能
- 在 `AdminRoute` 注册新路由
- 更新 `version.md` 记录接口说明与影响范围

ssx | 2026-05-20 | feat: dns默认服务配置修改 
ssx | 2026-05-20 | feat(dns): 新增域名同步接口并增强 DNS 管理查询与模型字段映射 - 在 `DnsToolController` 新增 `syncDomains` 方法，支持通过 `DnsDomainSyncRequest` 触发外部 DNS 域名同步
- 新增管理路由 `GET /domains/sync`，对接 `DnsToolController::syncDomains`
- 为同步接口补充异常处理：
  - 捕获 `RuntimeException` 并返回业务错误码/信息
  - 捕获通用异常并记录日志
- 优化 `DnsAdminService::domainIndex`：
  - 关联 `dns_provider_accounts`，返回 `account_name`
  - 增加 `ipBindings` 活跃绑定数量统计 `binding_ip_count`
  - 调整筛选条件字段为带表名前缀，避免联表歧义
- 更新 `DnsIpBinding` 模型类型转换：
  - 新增 `proxied`、`raw_record`、`synced_at`、`released_at` cast
- 同步更新相关接口文档（Provider 管理与 DNS 相关说明）

ssx | 2026-05-20 | feat(admin-dns): 重构 DnsToolController 并接入 DNS 管理能力 - 为控制器引入 `DnsAdminService`，并保留 `DnsToolService` 通过构造函数注入
- 新增并使用多种 Admin Request 校验类，覆盖 Provider/Account/Domain/IP 绑定/记录解析等场景
- 新增 DNS Provider 相关接口：列表、详情、新增、更新（基于 `DnsAdminService`）
- 统一接口返回为 `JsonResponse`，并使用 `CamelizeResource` 进行响应数据格式化
- 完善异常处理：区分 `BusinessException` 与通用异常，增加日志记录与标准错误响应
- 优化控制器注释与方法结构，提升可维护性与职责清晰度

ssx | 2026-05-19 | feat: 增加自动规则的执行返回信息 
ssx | 2026-05-19 | feat: 自动执行即使未触发条件也记录执行过程 
ssx | 2026-05-19 | refactor(automation): 在 Provider register 阶段一次性注入模块处理器 将 `AutomationServiceProvider` 的模块注册流程从 `boot()` 调整到 `register()`：
- 通过容器单例构造 `AutomationModuleRegistry` 时直接注入 handlers
- 当前默认注入 `TrafficPlatformAutomationService`
- 移除 `boot()` 中的逐个注册逻辑，避免重复注册与初始化路径分散

同时更新自动化开发文档，明确推荐在 Provider `register()` 阶段完成 handlers 一次性注入，并保留 `registerHandlers()/registerHandler()` 作为扩展入口。

ssx | 2026-05-19 | fix(automation): 调整模块处理器注册时机并补充自动化规则文档 - 将 `AutomationModuleRegistry` 的容器注册改为仅创建实例，不在 `register` 阶段绑定处理器
- 新增 `boot()` 注入 `AutomationModuleRegistry` 与 `TrafficPlatformAutomationService`，在启动阶段调用 `registerHandler` 完成注册
- 通过延后注册避免因容器初始化顺序导致 Registry 为空的问题
- 大幅完善自动化规则 API 文档：
  - 增加通用返回结构与字段说明
  - 补充 `module=traffic_platform` 专有返回与字段约束
  - 明确创建/更新规则的通用与模块专有说明
  - 补充执行接口（`/run`）通用字段与模块映射关系
  - 增加执行历史返回字段说明及 `traffic_platform` 指标快照说明
- 同步更新自动化规则开发指南文档

ssx | 2026-05-19 | feat(automation): 新增按模块查询可用模型接口并统一 module 规范化处理 - 在 `AutomationRuleController` 新增 `models` 方法，支持按 `module` 返回可用策略 model 标识列表
- 引入 `AutomationModelIndexRequest` 用于 `models` 接口参数校验
- `models` 接口返回中增加标准化后的 `module`，并对 `models` 结果使用 `CamelizeResource::collection` 包装
- 完善异常处理：区分业务异常与系统异常并记录日志
- 调整控制器注释文案，移除冗余路由示例，统一术语（如“启停状态”“每 module Redis 最新 100 条”）
- 在 `AutomationRuleService` 中对 `detail/store/update` 的 `module` 入参统一调用 `normalizeModule`
- 在 `store/update` 中回写标准化后的 `module` 到 `params`，确保落库与后续处理一致

ssx | 2026-05-19 | feat(automation): 统一自动化规则入口并按模块化方式调度执行 - 调整定时任务：将 `traffic-platform:automation-run` 替换为通用命令 `automation:run traffic_platform`，并同步优化注释描述
- 重构管理端路由：自动化规则接口从流量平台子路由中抽离为通用入口 `automation-rules`，改为使用 `AutomationRuleController`
- 新增执行记录查询接口：`GET /automation-rules/executions`
- 服务层改造：`AutomationRuleService` 从固定 `traffic_platform` 常量切换为显式传入 `module`，并通过 `AutomationModuleRegistry` 校验模块处理器
- 规则查询与详情能力升级为“按模块”通用实现

ssx | 2026-05-19 | feat(traffic-platform): 新增自动化规则管理路由并接入定时与 Horizon 队列 - 在管理端路由中新增 `automation-rules` 分组及相关接口：
  - `GET /traffic-platform/automation-rules`
  - `GET /traffic-platform/automation-rules/detail`
  - `POST /traffic-platform/automation-rules/create`
  - `POST /traffic-platform/automation-rules/update`
  - `POST /traffic-platform/automation-rules/update-status`
  - `POST /traffic-platform/automation-rules/run`
- 在 `Kernel` 中新增 `traffic-platform:automation-run` 定时任务，设置为每 5 分钟执行一次，并启用单机与防重叠控制
- 更新 Horizon 配置，新增 `automation` 队列等待时间与生产环境 supervisor：
  - 新增 `redis:automation` waits 配置
  - 新增 `automation` supervisor（单进程、超时 300、重试 2 次、退避策略）
  - 本地 `NxPanel` 队列列表中加入 `automation`
- 更新 `version.md` 记录 Traffic Platform 自动化能力一期相关说明

ssx | 2026-05-19 | feat(traffic-platform): 为平台列表新增分页支持并补充接口文档 - 在平台列表请求参数中新增 `page`、`pageSize` 校验规则（`page>=1`，`pageSize` 范围 `1-200`）
- 服务层实现分页查询：增加 `total` 统计，并通过 `offset + limit` 返回当前页数据
- 控制器响应新增分页字段：`page`、`pageSize`、`total`，保留 `data` 列表
- 更新 API 文档：
  - 补充平台列表分页参数与完整返回结构示例
  - 明确返回字段说明（含 `data[]` 字段）
  - 补充启用/禁用接口返回字段说明
  - 完善账号列表/详情返回结构与字段说明

ssx | 2026-05-18 | feat(admin): 优化同步接口鉴权与收入同步日期参数处理 - 同步触发接口改为通过 `Authorization` 请求头传递 `secret_key`，不再使用 POST body 传参
- 调整收入同步接口日期参数为可选（`start_date`、`end_date` 需同时传入）
- 新增日期参数手动校验逻辑：缺失配对返回 422，格式错误返回统一提示
- 收入同步请求改为按需拼接查询参数到 URL，并发送无 body 的 POST 请求
- 移除对 `ValidationException` 的捕获，改为显式校验流程控制
- 引入 `Validator` facade 以支持条件化日期校验

ssx | 2026-05-15 | feat(ad-spend): 为平台登录 token 增加 Redis 缓存并统一 1 小时过期策略 - 在 `AdSpendPlatformService` 中引入 Redis 缓存，新增 token 缓存 key：`ad_spend:platform:token:{accountId}`
- 登录流程优化为优先读取 Redis token，命中则直接返回，减少重复登录请求
- 当 Redis 未命中但数据库 token 仍有效时，回填 Redis 缓存并复用该 token
- 登录成功后写入 Redis；若接口未返回过期时间，则默认按 1 小时设置 `token_expired_at`
- 新增 token 缓存相关私有方法（读取、写入、key 生成）及统一 TTL 常量
- 更新 `version.md`，补充改动说明、影响范围与迁移说明

ssx | 2026-05-15 | refactor(admin/firebase): 统一 Firebase Analytics 控制器命名空间到 Firebase 子模块 将多个 Firebase Analytics 相关控制器的命名空间由 `App\Http\Controllers\V3\Admin` 调整为 `App\Http\Controllers\V3\Admin\Firebase`，以匹配目录结构

ssx | 2026-05-15 | feat(project): 新增项目日汇总同步/异步接口并清理冗余方法 - 在 `ProjectController` 新增 `aggregate` 接口，支持按 `startDate/endDate` 触发 `project:aggregate-daily` 命令并返回执行结果
- 在 `ProjectController` 新增 `aggregateAsync` 接口，支持按日期范围投递 `AggregateProjectDailyJob` 异步任务并返回 `triggerId`
- 为两个汇总接口补充参数校验、异常捕获与日志记录（含 422/500 响应）
- 移除 `AdSpendPlatformController` 中未使用的 `projectDaily` 方法，减少重复入口
- 更新项目相关文档与资源返回示例，补充用户 App 绑定等接口说明

ssx | 2026-05-15 | fix: 命名空间问题 
ssx | 2026-05-15 | feat: 增加文档和调整文件 
ssx | 2026-05-14 | feat: 新增节点质量排行接口；fix: 性能探测聚合错误码超长截断 - 路由：添加 `/nodes/quality-rank` 接口，关联 FirebaseAnalyticsNodeController
- 修复：`perf:aggregate` 命令对 `error_code` 超长内容截断至 64 字符，避免写入失败

ssx | 2026-05-14 | refactor: 重构项目广告账号控制器并更新路由规范 - 将 `ProjectAdAccountController` 的查询方法从路径参数改为查询参数，引入 `ProjectAdAccountService` 服务层
- 更新 `projectDaily` 方法，将 `projectCode` 从路由参数改为请求体输入
- 补充 `AGENTS.md` 架构规范：禁止 Controller 中使用 `$request->validate`、方法增加注解
- 统一 API 路由设计文档：导出接口与导入接口的路径规范，嵌套资源改用查询参数

ssx | 2026-05-12 | refactor: 重构项目控制器，使用 FormRequest 和 Service 层分离业务逻辑 
ssx | 2026-05-12 | feat: 文档迁移调整 
ssx | 2026-05-12 | feat(project): 新增人均展示(impressionsPerUser)和每用户平均收入(arpu)指标计算及文档 
ssx | 2026-05-12 | fix: 补充account_label 
ssx | 2026-05-12 | feat: App列表查询增加accountLabel字段返回 
ssx | 2026-05-12 | refactor: 提取 fetchApps 请求参数验证到 AdPlatformAppFetch 请求类 
ssx | 2026-05-12 | feat(ad-revenue): 新增 ad_platform_app 列表查询接口；fix(aggregate): 优化每日数据聚合逻辑，增加旧数据清理和启用项目过滤 
ssx | 2026-05-12 | feat(ProjectAggregate): 新增总花费(totalCost)字段，支持排序与查询 - 控制器中新增 totalCost 计算字段 (ad_spend_cost + traffic_cost)
- 请求验证器中增加 totalCost 作为允许的排序字段
- 更新 API 文档与指标列表，添加 totalCost 字段说明

ssx | 2026-05-12 | fix: 增加节点上报无用户流量数据时的计算数默认值处理 
ssx | 2026-05-12 | fix(ReplayUserReportRawFromOss): --clear-day选项增加清空多个关联表，防止回放数据重复 
ssx | 2026-05-11 | feat: 支持按注册元数据筛选用户及更新注册元数据字段 
ssx | 2026-05-11 | feat: 清理增加时间间隔阈值 
ssx | 2026-05-11 | feat: 增加归档文件清理操作 
ssx | 2026-05-11 | feat(node-server-report): 添加 --skip-archive 选项以支持回放重算时跳过归档 
ssx | 2026-05-11 | feat(report): 支持日期范围重建小时报表，优化日志输出 - 为 rebuild_report_hourly:rebuild 添加 --to 选项，支持指定结束日期，循环处理日期范围内的每一天
- 增加日期范围有效性校验和错误提示
- 优化 ReplayNodeServerReportRawFromOss 日志输出，显示桶和负载数量，空负载跳过处理

ssx | 2026-05-11 | refactor(ReplayUserReportRawFromOss): 提取时间戳解析至UserReportService并增加元数据数组校验处理 
ssx | 2026-05-11 | refactor: 精简 ReplayUserReportRawFromOss 命令，移除 `--minute` 和 `--batch` 选项 - 移除 `--minute` 及对应的分钟校验逻辑，简化粒度控制
- 移除 `--batch` 选项，不再分批处理（相关代码已内联）
- 调整 `--clear-day` 行为：仅清除 v3_user_report_node 表，不再清除汇总/用户/失败表，并移到聚合后执行
- 将报告时间解析从外部服务调用改为类内方法 `resolveReportAtMs`

ssx | 2026-05-11 | feat: 在每小时聚合报告中添加 app_id 和 app_version 字段 
ssx | 2026-05-11 | fix: 修改流量使用时间字段uint为ulong，避免溢出 
ssx | 2026-05-09 | feat: 移除旧版节点报表相关功能并更新文档 
ssx | 2026-05-09 | feat: 新增统一小时报表聚合功能及相关查询接口 - 新增 `report_hourly:aggregate` 定时任务，每5分钟重算当前小时和上一小时数据
- 新增 `ReportHourlyAggregateCommand` 命令，支持节点和用户小时报表的统一聚合
- 新增 `v3_report_node_hourly` 和 `v3_report_user_hourly` 数据库迁移及模型
- 新增 `queryNodeReport` 接口，支持节点小时报表的多维度分组、过滤和分页查询
- 新增 `queryUserReportHourly` 接口，支持用户小时报表的多维度分组、过滤和分页查询
- 新增 `NodeReportQueryRequest` 和 `UserReportHourlyQueryRequest` 请求验证类
- 新增路由 `POST /report/nodeReport/query` 和 `POST /report/userReport/hourly/query`
- 聚合逻辑支持加权平均（CPU、内存、速率等）和最大值（峰值指标）计算

ssx | 2026-05-09 | fix: 重命名数据库表名以统一命名规范 将 `v3_user_report_node_summary` 重命名为 `v3_user_report_node`，将 `v3_user_report_traffic` 重命名为 `v3_user_report_user`，同步更新所有相关引用，包括数据聚合命令、数据回放命令、管理报表控制器

ssx | 2026-05-08 | feat: 添加节点服务器报告默认值常量与数据恢复功能 - 新增默认节点ID、类型、主机和公网IP常量
- 实现restoreBucket方法用于从备份恢复Redis数据桶
- 统一使用默认常量替换硬编码空字符串值
- 改进节点元数据处理逻辑，确保字段始终具有有效默认值
- 增强数据库更新操作的空值处理

ssx | 2026-05-08 | feat: 新增节点上报数据采集与报表查询功能 - 在 Console Kernel 中新增节点上报数据派发定时任务，每5分钟执行一次归档和队列投递
- 在 ServerController 中增加节点上报原始数据推送功能，包含流量、在线状态、性能指标等数据
- 新增 ReportController 节点上报报表查询接口，支持节点维度和用户维度的数据查询与聚合
- 添加相关请求验证类 NodeServerReportNodeQueryRequest 和 NodeServerReportUserQueryRequest
- 实现节点上报数据的维度筛选、分组统计和分页查询功能

ssx | 2026-05-08 | feat: 移除节点实时上报接口的节点过滤和关联查询 - 删除 ReportController 中 nodeServerRealtime 方法的节点查询和过滤逻辑
- 移除 NodeServerRealtimeRequest 中的 nodeId 必填校验规则
- 更新接口文档，移除 nodeId 请求参数和节点相关返回字段
- 接口改为返回全部实时队列数据的分页结果

ssx | 2026-05-08 | feat：提取维度选择逻辑到独立方法 将重复的维度选择逻辑提取到新的私有方法 `buildDimensionSelect` 中，该方法根据维度类型和是否填充未知值参数动态生成 SQL 选择语句。对于字符串类型的维度，当需要填充未知值时使用 COALESCE 函数处理空值，其他情况直接返回原始字段。

ssx | 2026-05-08 | feat: 新增节点实时上报数据缓存与查询接口 在 ServerController 中新增缓存逻辑，将节点上报的最新500条数据存入缓存，包含节点ID、类型、IP、流量、存活状态、在线状态、状态指标与时间戳。
在 ReportController 新增 nodeServerRealtime 方法，支持分页查询指定节点的实时上报缓存数据，并返回节点基本信息与数据列表。
在 AdminRoute 中注册 /report/nodeServer/realtime 路由，对应新增的查询接口

ssx | 2026-05-08 | feat: 新增项目聚合数据查询接口 在ProjectAggregateController中新增queryDaily方法，支持按日期范围、项目代码、国家等多维度查询项目日报表数据，提供分页、排序和分组聚合功能

ssx | 2026-05-08 | feat: 为报表接口添加自定义排序支持 在报表控制器中增加排序参数处理，包括：
- 从请求中获取 orderBy 和 orderDirection 参数
- 新增 normalizeOrderDirection 方法规范化排序方向
- 为三种报表类型（用户报表汇总、节点报表汇总、流量报表）分别定义可排序字段列表
- 支持按指定字段和方向排序，未指定时保持原有默认排序逻辑

ssx | 2026-05-07 | feat: 重构广告投放平台日报接口 - 使用表单请求类 AdSpendPlatformDailyQueryRequest 验证参数
- 引入服务层 AdSpendDailyReportService 处理查询逻辑
- 采用 CamelizeResource 统一响应字段命名规范
- 增加异常处理与日志记录
- 更新接口文档，调整参数结构为 filters 嵌套格式
- 响应数据增加分页信息和日期范围字段
- 支持多维分组统计，分组时返回聚合数据

ssx | 2026-05-07 | feat: 增强性能报告和用户报告的数据解析功能 - 为时间字符串添加正则解析支持，可识别 HH:MM:SS 和 MM:SS 格式并转换为秒数
- 改进存储容量单位解析逻辑，使用更精确的单位匹配方式替换原有的字符串包含检测
- 统一处理文本中的空格和逗号分隔符，提升数值提取的准确性
- 扩展支持的存储单位类型，包括字节(byte/bytes)、千字节(k/kb/kib)、兆字节(m/mb/mib)和千兆字节(g/gb/gib)
- 在 AggregatePerformanceReports 和 AggregateUserReport 两个命令类中同步实现

ssx | 2026-05-07 | feat: 重构元数据解析逻辑并优化调试输出 在多个命令中提取元数据解析逻辑为统一的私有方法 `resolveMetadata`，用于处理数组或JSON字符串格式的元数据。同时，在 `DebugUserReportVpnFromOss` 命令中增加了对解析载荷和包含 `user_default` 字段的载荷的计数。

ssx | 2026-05-07 | feat: 更新流量聚合方法以支持多维度统计 修改 `aggregateTraffic` 方法，增加 `$payloads` 参数用于处理原始上报数据。新增基于用户ID、应用ID、版本、国家和时间维度的分组逻辑，初始化流量统计相关字段。

ssx | 2026-05-07 | feat: 为聚合用户报告命令添加跳过归档选项并优化重放命令处理逻辑 - 在 AggregateUserReport 命令中添加--skip-archive 选项，支持跳过OSS原始归档操作
- 修改归档逻辑，当启用跳过归档选项时直接标记为已跳过状态
- 优化 ReplayUserReportRawFromOss 命令的处理流程：
  - 改为逐行读取OSS文件内容并解析JSON数据
  - 根据上报时间自动计算桶时间并进行过滤
  - 按桶时间分组payload数据，提高重放处理效率
- 改进时间解析逻辑，增强参数类型安全性

ssx | 2026-05-07 | Merge branch 'main' of https://github.com/Lawer09/nxpanel 
ssx | 2026-05-07 | feat: 支持多VPN连接数据处理 - 将extractVpnConnectData返回类型改为数组，支持多个VPN连接数据
- 新增对空报告情况下多VPN连接数据的循环处理
- 为每个VPN连接项生成独立的数据记录
- 调整用户ID提取逻辑，使用主VPN连接数据的用户ID字段

ssx | 2026-05-07 | feat: 增加日志 
ssx | 2026-05-07 | feat: 在用户聚合报告中增加成功与失败统计功能 - 在AggregateUserReport命令中新增success_count和fail_count字段，用于统计VPN连接成功与失败次数
- 根据status字段值动态增加成功或失败计数
- 更新数据库插入逻辑，包含新增的统计字段
- 重构extractVpnConnectionData方法以支持多条目处理
- 在ReportController中增加成功率计算字段(success_rate)
-  调整查询语句，包含新增统计字段的汇总和成功率计算

ssx | 2026-05-07 | feat: 重构用户报告OSS存储路径格式 - 将OSS原始数据存储路径从`user-report/raw/dt=YYYY-MM-DD/hour=HH/minute=mm/bucket={bucket}/part-{ts}-{rand}.ndjson`改为扁平化结构`user_report/raw/YYYY/MM/DD/HH-mm-ss_{rand}.ndjson`
- 更新AggregateUserReport命令中的路径生成逻辑，使用新的时间格式
- 修改ReplayUserReportRawFromOss命令，适配新的路径格式并重构元数据提取方法

ssx | 2026-05-07 | fix: 投放拉取接口调整 
ssx | 2026-05-07 | fix: 调整日志输出 
ssx | 2026-05-07 | feat: 新增用户上报聚合调度与查询接口 - 在Console Kernel中添加用户上报聚合调度任务，每5分钟执行一次，支持单服务器运行且防止重叠
- 新增PerformanceController中的用户上报汇总查询接口，支持多条件筛选与版本化缓存
- 实现流量数据查询接口，包含时间范围、应用版本、国家等多维度过滤
- 添加缓存版本管理机制

ssx | 2026-05-07 | feat: 增强VPN连接数据聚合处理 - 新增原始payload归档至OSS功能，便于后续审计回放
- 修复流量聚合逻辑，使用rawRecords替代probeNodeRecords确保客户端流量数据完整入库
- 扩展VPN连接数据处理能力，新增状态、阶段、错误码、使用时长及流量等字段解析
- 优化空报告记录处理，支持通过VPN节点IP解析节点ID
- 完善元数据字段提取，增加VPN相关原始数据

ssx | 2026-05-07 | feat: 修复时区处理问题，统一使用UTC时间戳并设置应用时区 在性能报告聚合命令中，将时间戳处理方法从`createFromTimestampMs`改为`createFromTimestampMsUTC`，确保时间戳始终基于UTC时间处理。同时为生成的时间对象设置应用配置时区（默认为Asia/Shanghai）。

ssx | 2026-05-07 | Merge branch 'main' of https://github.com/Lawer09/nxpanel 
ssx | 2026-05-07 | feat: 为性能报告聚合命令添加临时调试表写入功能 - 新增 `--temp` 选项用于同时写入 `*_temp` 调试表
- 在聚合性能数据时支持写入 `v2_node_performance_aggregated_temp` 临时表
- 在聚合探测指标时支持写入 `v2_node_probe_aggregated_temp` 临时表
- 为用户上报次数统计添加临时表写入支持

ssx | 2026-05-07 | feat: 聚合日志输出位置调整 
ssx | 2026-05-06 | feat: 统计变量获取来源 
ssx | 2026-05-06 | feat: 修改 OSS 启用判断逻辑，从环境变量迁移至配置文件 将 OSS 启用状态的判断从直接读取环境变量改为从配置文件获取，并在配置文件中新增 enabled 字段，统一管理 OSS 相关配置

ssx | 2026-05-06 | feat: 新增从 OSS 回放性能数据并重新聚合的命令 
ssx | 2026-05-06 | feat: 统一性能报告时间戳处理逻辑 新增 resolveMetadataTimestampMs 方法统一处理元数据时间戳，确保 metadata_timestamp_ms 字段在所有报告记录中的一致性。重构 resolveTrafficBucket、resolveEventBucket 和 resolvePayloadBucket 方法，使用 resolveTrustedTimestampMs 替代原有的多重时间戳回退逻辑。优化单条记录处理流程，增加 metadata_timestamp_ms 和 event_timestamp_ms 字段填充

ssx | 2026-05-06 | feat: 为用户列表接口添加今日流量统计功能 在UserController的用户列表查询中新增今日流量统计功能：
- 从v2_node_traffic_aggregated表中聚合查询用户今日总流量
- 将查询结果映射到用户ID对应的流量数据
- 在用户数据转换时添加report_traffic字段显示今日流量统计
- 处理用户ID不存在时的默认值情况

ssx | 2026-05-06 | feat: 在聚合性能报告中添加用户ID字段并优化数据清理逻辑 - 在AggregatePerformanceReports命令中增加user_id字段到数据聚合和清理逻辑
- 注释掉v2_node_traffic_aggregated表的删除操作，改为保留历史流量数据
- 在UserController中添加用户报表功能，支持按日期范围和多维度筛选查询用户报告统计

ssx | 2026-05-06 | feat: 重构性能报告聚合逻辑，支持动态时间桶解析 - 移除硬编码的时间桶计算逻辑，改为基于事件时间戳动态解析时间桶
- 新增 `resolveEventBucket` 方法用于统一处理时间桶解析
- 新增 `normalizeTimestampMs` 方法用于时间戳标准化
- 在记录中添加 `event_timestamp_ms` 字段保存原始事件时间戳
- 简化 `aggregateProbeMetrics` 和`aggregateUserReportCount` 方法签名，不再需要传入时间参数
- 优化日志记录结构，移除冗余的时间字段

ssx | 2026-05-06 | feat: 优化聚合项目日报数据命令的默认日期范围和数据累加逻辑 - 将默认聚合日期从仅当天改为昨天和当天
- 修复聚合逻辑：将覆盖式赋值改为累加式处理，避免多源数据时统计遗漏
- 更新命令描述以反映默认日期范围

ssx | 2026-05-06 | feat: 在广告支出平台控制器中增加未匹配记录统计 新增$unmatchedRecords变量用于统计未匹配记录数量
将硬编码的未匹配记录值0替换为动态统计变量$unmatchedRecords

ssx | 2026-04-30 | fix 
ssx | 2026-04-30 | feat: 添加投放日报同步任务并重构渠道参数处理 在 Kernel.php 中添加每小时执行的投放日报同步任务，使用 onOneServer 和 withoutOverlapping 保证任务唯一性

重构 AuthController 中的渠道参数验证结构，将渠道相关字段从 metadata 平级改为 channel 子数组，包括：
- channel_type
- utm_source
- utm_medium
- utm_campaign
- raw_referrer
- click_ts
- install_begin_ts

调整 LoginService 中的元数据处理逻辑，简化允许的字段列表，仅保留基础信息和 channel 字段，移除冗余的 channelType 兼容处理

ssx | 2026-04-30 | feat: 新增流量成本计算与聚合逻辑 在AggregateProjectDailyData命令中新增流量成本计算功能，主要包含以下改进：

1. 新增流量数据查询方法queryTrafficUsageMbByCountry，用于获取各项目分国家的流量使用数据
2. 整合流量项目代码到聚合处理流程中，确保所有启用流量平台账号的项目都被纳入计算
3. 实现流量成本计算逻辑，按照每MB 0.16/1024美元的标准计算流量成本
4. 扩展聚合指标计算，新增：
   - 流量使用量(traffic_usage_mb)
   - 流量成本(traffic_cost)
   - 总成本(total_cost，包含广告支出和流量成本)
   - 利润(profit，收入减去总成本)
   - 投资回报率(roi)

同时优化了聚合键处理逻辑，确保所有维度的数据都能正确合并计算。

Co-authored-by: Copilot <copilot@github.com>

ssx | 2026-04-29 | fix 
ssx | 2026-04-29 | fix 
ssx | 2026-04-29 | feat: 防止流量数据重复累加 在项目日粒度数据聚合命令中引入流量写入标记集合，确保每个日期+项目组合的流量使用量仅统计一次。通过trafficWrittenProjectSet避免因国家/用户维度细分导致的流量数据重复累加问题

ssx | 2026-04-29 | add country 
ssx | 2026-04-29 | fix 
ssx | 2026-04-29 | feat: 在用户指标聚合中添加用户国家字段并优化查询逻辑 - 在buildUserMetrics方法中新增user_country字段，使用normalizeCountry方法统一处理国家代码
- 重构用户查询逻辑，增加对register_metadata为空或无效值的备选查询方案
- 移除冗余的dailyReportCountry查询，直接使用规范化后的目标国家进行过滤
- 在返回数组中统一包含user_country字段，确保数据一致性

ssx | 2026-04-29 | fix: project_platform_app_map 
ssx | 2026-04-29 | feat: 重构项目数据聚合逻辑，优化关联查询和用户指标统计 - 将 `AggregateProjectDailyData` 命令中的 `project_platform_app_map` 表关联替换为 `project_ad_platform_accounts`，支持更灵活的账户和应用绑定类型判断
- 重构用户指标统计方法，使用新的 `project_user_app_map` 表直接获取应用ID，简化查询逻辑
- 在路由文件中将 `ProjectMappingController` 更新为 `ProjectUserAppMapController`，反映功能调整

ssx | 2026-04-29 | fix 
ssx | 2026-04-29 | feat: 移除流量平台相关备注字段并修复流量单位转换错误 - 移除TrafficPlatformAccountController中的remark字段验证和存储逻辑
- 移除TrafficPlatformController中的remark字段验证和存储逻辑
- 修复AggregateProjectDailyData中的流量单位转换错误：将traffic_mb转换为GB时除以1024
- 清理不再使用的备注字段相关代码

ssx | 2026-04-29 | fix 
ssx | 2026-04-29 | fix 
ssx | 2026-04-29 | Merge branch 'main' of https://github.com/Lawer09/nxpanel 
ssx | 2026-04-29 | feat: 增强性能报告聚合功能并新增节点流量与探测指标统计 - 新增 `Server` 模型和 `Cache` 门面的引入
- 分离内部节点记录与探测节点记录处理逻辑
- 新增 `aggregateProbeMetrics` 方法用于聚合探测状态与错误码指标
- 新增 `aggregateTrafficMetrics` 方法用于节点流量聚合分析
- 调整用户上报次数统计方法，改为基于原始负载数据统计
- 扩展扁平化记录字段，新增节点IP、状态、探测阶段、错误码等字段
- 新增外部节点IP到内部node_id的映射解析功能
- 优化节点性能查询接口，支持按节点ID和日期范围过滤

ssx | 2026-04-29 | use data replace list 
ssx | 2026-04-29 | fix orderby 
ssx | 2026-04-29 | fix 
ssx | 2026-04-28 | feat: 为项目聚合控制器添加分组查询功能 在 ProjectAggregateController 的 daily 方法中新增以下功能：
- 添加 groupBy 参数支持 detail/dateProject/dateProjectCountry 三种分组模式
- 新增 normalizeQueryParams 方法处理查询参数标准化
- 实现按日期+项目+国家的多维度聚合统计
- 添加聚合计算字段包括：ECPM、CTR、匹配率、展示率、ROI、CPI 等核心指标
- 保持原有分页功能的同时支持聚合查询的分页统计

ssx | 2026-04-28 | fix: 流量成本计算修改为0.16单价 
ssx | 2026-04-28 | fix 
ssx | 2026-04-28 | fix: regUsers count error 
ssx | 2026-04-28 | feat: 新增项目聚合接口支持手动触发和异步处理 在ProjectAggregateController中新增两个接口：
1. aggregate方法：支持同步执行项目聚合任务，调用Artisan命令处理指定日期范围的数据
2. aggregateAsync方法：支持异步处理，通过队列任务执行聚合操作

路由文件中添加对应的POST路由：
- /project-aggregates/aggregate
- /project-aggregates/aggregate-async

文档文件doc5.md同步更新，详细说明了接口的使用方法、请求参数和返回格式

ssx | 2026-04-28 | fix 
ssx | 2026-04-28 | fix 
ssx | 2026-04-28 | fix 
ssx | 2026-04-28 | 补充查询 
ssx | 2026-04-28 | fix 
ssx | 2026-04-28 | feat: 在广告投放平台控制器中新增查询参数标准化方法 在AdSpendPlatformController中新增normalizeQueryParams方法，用于将驼峰命名的查询参数转换为下划线命名格式，提升API参数兼容性。同时在daily、summary、trend、projectCodes、projectDaily等方法中调用此方法，确保请求参数统一处理。此外优化日期查询逻辑，仅在参数存在时添加日期条件过滤，提高查询灵活性。

ssx | 2026-04-28 | Merge branch 'main' of https://github.com/Lawer09/nxpanel 
ssx | 2026-04-28 | feat: 添加项目日报聚合定时任务 在控制台内核调度中新增项目日报聚合定时任务，每5分钟执行一次project:aggregate-daily命令，使用单服务器模式并设置4分钟互斥锁防止重复执行。

ssx | 2026-04-28 | fix 
ssx | 2026-04-28 | feat: 移除项目ID字段，统一使用项目代号作为关联标识 - 在AdSpendPlatformController中更新updateOrCreate方法，将project_id替换为project_code
- 从AdSpendDailyReport模型移除project_id字段的整型转换
- 迁移文件中移除project_traffic_platform_accounts、project_ad_platform_accounts和ad_spend_daily_reports表的project_id字段
- 更新ad_spend_daily_reports表的唯一约束和索引，使用project_code替代project_id
- 确保数据表一致性，所有关联操作均通过project_code进行

ssx | 2026-04-28 | feat: 新增广告投放平台管理功能 在AdminRoute.php中新增AdSpendPlatformController的导入和路由配置，包含以下功能：
- 账户管理：获取账户列表、详情、保存、更新、状态修改和测试接口
- 数据同步：同步接口、同步任务查询和详情接口
- 报表查询：日报、汇总和趋势报表接口
- 项目代码查询接口
- 在项目广告账户路由中新增项目维度广告投放汇总、趋势和日报接口

在AdSpendPlatformAccount模型中新增密码字段的加密解密访问器，实现密码的安全存储和读取：
- getPasswordAttribute方法对加密密码进行解密
- setPasswordAttribute方法对明文密码进行加密存储

ssx | 2026-04-28 | feat: 新增项目管理路由及API文档 在AdminRoute.php中新增项目管理相关路由，包括项目基础信息、流量账号和广告账号的增删改查接口。同时更新doc3.md文档，补充投放平台API接口说明，涵盖账号配置、同步任务管理等功能模块。

ssx | 2026-04-28 | fix 
ssx | 2026-04-28 | feat: 新增项目广告账号关联管理功能 新增ProjectAdAccountController控制器，包含以下功能：
- 查询项目已关联广告账号接口，支持按平台和启用状态筛选，补充账号名称信息
- 新增项目广告账号关联接口，包含参数验证、唯一性校验和数据保存
- 更新项目广告账号关联状态接口，支持启用/禁用操作
- 删除项目广告账号关联接口

ssx | 2026-04-28 | feat: 优化性能报告清理逻辑并新增流量平台查询接口 - 在 AggregatePerformanceReports 中注释用户上报统计数据删除逻辑，保留历史查询能力
- 在 PerformanceController 中新增新用户统计缓存功能，基于用户首次上报时间计算新用户数
- 新增流量平台使用量查询接口，支持分页查询、月度汇总和流量趋势统计
- 添加相关缓存机制和 SQL 查询优化

ssx | 2026-04-26 | feat: 添加服务器负载状态历史记录和性能报表增强功能 - 在ServerController中新增服务器负载状态历史缓存功能，记录并清理一小时内的历史数据
- 在PerformanceController中为节点聚合报表补充节点名称和类型信息
- 新增getUserHourlyStats方法提供24小时内用户新增与活跃统计数据
- 优化数据结构处理和缓存管理逻辑

ssx | 2026-04-24 | feat: 重构广告收益控制器并优化性能控制器数据返回格式 - 移除AdRevenueController中硬编码的聚合维度白名单，改为使用GROUP_BY_COLUMN_MAP映射表
- 新增mapGroupByColumns方法处理camelCase到snake_case的字段名转换
- 优化排序逻辑，支持映射后的数据库列名排序
- 在PerformanceController中统一使用CamelizeResource格式化

ssx | 2026-04-24 | Merge branch 'main' of https://github.com/Lawer09/nxpanel 
ssx | 2026-04-24 | feat: 使用 CamelizeResource 统一返回 Co-authored-by: Copilot <copilot@github.com>

ssx | 2026-04-24 | fix 
ssx | 2026-04-24 | feat: 统一AdAccount和AdRevenue参数和返回值 
ssx | 2026-04-24 | feat: 统一参数和返回值 
ssx | 2026-04-24 | fix 
ssx | 2026-04-24 | feat: 重构同步服务器更新接口并新增测试同步功能 - 引入 SyncServerUpdate 表单请求类处理更新参数验证
- 移除控制器内手动验证逻辑，改用请求类 validated 方法
- 新增 testSync 方法用于向同步服务器发送测试触发请求
- 扩展 SyncServerSave 请求规则，增加 secret_key 和 port 字段验证
- 完善错误处理，增加连接超时等特定异常捕获

ssx | 2026-04-24 | feat: 使用表单请求验证和分页优化项目映射查询接口 - 将 `fetch` 方法参数从 `Request` 改为 `ProjectMappingFetch` 表单请求类，实现自动验证
- 使用 `$request->validated()` 获取已验证参数，替换原有的手动参数提取
- 使用 Laravel 分页器 `paginate()` 替代手动分页逻辑，简化代码结构
- 统一响应数据格式，返回分页器提供的标准分页数据结构
- 保持错误日志记录机制不变，确保异常处理可靠性

ssx | 2026-04-24 | feat: 优化同步监控控制器，使用表单请求验证和分页查询 - 在states方法中引入SyncStateFetch表单请求验证，替换原有的Request参数获取方式
- 将同步状态查询从直接获取改为分页查询，返回结构包含数据列表、总数、当前页码和每页数量
- 在logs方法中引入SyncLogFetch表单请求验证，优化参数处理逻辑
- 同步日志查询改用分页方式，统一返回格式与状态查询保持一致
- 新增SyncLogFetch和SyncStateFetch请求类的引入，增强参数验证能力

Co-authored-by: Copilot <copilot@github.com>

ssx | 2026-04-24 | feat: 添加性能日志中间件并配置相关路由和日志通道 - 在HTTP内核中注册性能日志中间件perf.log
- 为性能相关路由组添加perf.log中间件
- 新增performance日志通道配置，日志保存30天

ssx | 2026-04-23 | 修复广告账户更新时可能清空credentials_json的问题 将原有的条件判断逻辑改为使用array_intersect_key方法，仅保留前端实际传递的字段，避免未传递的nullable字段被意外设置为null值

Co-authored-by: Copilot <copilot@github.com>

ssx | 2026-04-23 | feat: 移除凭据加密逻辑并统一凭据字段校验规则 - 删除 AdAccountUpsert 请求中凭据字段的条件校验逻辑，统一改为 required
- 移除 AdPlatformAccount 模型中的凭据加密解密方法，改为直接存储和返回 JSON 字符串
- 不再隐藏凭据字段，响应中将返回原始凭据数据

Co-authored-by: Copilot <copilot@github.com>

ssx | 2026-04-23 | feat: 优化性能报告与广告账户管理逻辑 - 修复性能报告中延迟值为负数时自动归零的处理
- 移除广告账户创建成功时的冗余状态码返回
- 添加广告账户更新时credentials_json字段的空值保护逻辑
- 调整广告账户校验规则，编辑时允许credentials_json为空
- 放宽性能报告delay字段的校验范围，移除最小最大值限制

Co-authored-by: Copilot <copilot@github.com>

ssx | 2026-04-23 | feat: 添加工单控制器路由支持 在UserRoute.php中引入TicketController并新增以下工单相关路由：
- POST /ticket/reply 用于工单回复
- POST /ticket/close 用于关闭工单
- POST /ticket/save 用于保存工单
- GET /ticket/fetch 用于获取工单
- POST /ticket/withdraw 用于撤回工单

ssx | 2026-04-23 | feat: 添加广告收入报表路由组 在AdminRoute中新增AdRevenueController的引入，并添加广告收入报表相关路由组，包含以下端点：
- GET /ad-revenue/fetch 获取广告收入数据
- POST /ad-revenue/aggregate 聚合广告收入数据
- GET /ad-revenue/trend 获取广告收入趋势
- GET /ad-revenue/summary 获取广告收入摘要
- POST /ad-revenue/top-rank 获取广告收入排名

Co-authored-by: Copilot <copilot@github.com>

ssx | 2026-04-23 | feat: 调整数据清理策略和凭据加密方式 - 将聚合报告数据清理时间从14天延长至30天
- 移除广告账户控制器的应用层凭据加密逻辑
- 在广告账户模型中实现自动加密解密的访问器方法
- 简化代码注释和结构

Co-authored-by: Copilot <copilot@github.com>

ssx | 2026-04-23 | feat: 新增广告账户、项目映射与同步服务相关路由 在 AdminRoute 中新增以下功能模块：
- 引入 AdAccountController、ProjectMappingController、SyncServerController 和 SyncMonitorController 控制器
- 添加广告账户管理路由，支持查询、保存、更新、状态修改、凭证测试及批量分配服务器
- 新增项目应用映射路由，支持查询、保存、更新和状态修改
- 新增同步服务器管理路由，支持查询、保存、更新和状态修改
- 添加同步监控路由，包括状态查询、日志查看和任务触发功能
- 新增内部 API 路由，支持同步服务器心跳上报（不经过 admin 中间件）

ssx | 2026-04-23 | feat 使用新的上报解析格式 
ssx | 2026-04-21 | feat: 新增应用客户端凭证同步任务并增强性能数据查询与日志记录 - 在 Console Kernel 中添加每分钟执行的应用客户端凭证同步到 Redis 的任务，使用单服务器运行并避免重叠执行
- 在 ServerController 中优化 Log 导入方式并新增节点上报数据的详细日志记录，包括流量、存活、在线数量和状态信息
- 为 PerformanceController 的聚合查询接口新增 group_by 参数支持，允许按节点、国家、ISP、平台、应用版本、日期和小时等多维度聚合统计成功率和延时数据
- 重构聚合查询逻辑，支持加权平均计算

ssx | 2026-04-20 | feat: 扩展性能聚合与查询维度 新增按客户端 ISP、应用 ID 与版本聚合与筛选，并补充汇总记录字段

ssx | 2026-04-20 | feat(perf): 按5分钟桶聚合上报并统计用户次数 - 从桶中弹出并记录 bucket key
- 新增用户上报次数聚合与两周前数据清理
